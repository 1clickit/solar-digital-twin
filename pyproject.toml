[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "solar-digital-twin"
version = "0.1.0"
description = "Modular Solar Digital Twin platform"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
